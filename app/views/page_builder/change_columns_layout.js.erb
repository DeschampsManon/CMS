$("#column-modal").modal("hide");

var $parent = $(".current-container .module-container .row");

$parent.empty();

<% eval(@layout).each do |layout| %>
	$parent.append("<%= j render 'page_builder/layouts/module_container', {column_width: layout } %>");
<% end %>
